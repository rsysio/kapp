workspace:
    base: /go
    path: src/github.com/rsysio/kapp

pipeline:

    build:
        image: golang:latest
        commands:
            - go install glide
            - glide install
            - go build

    publish:
        image: ubuntu
        commands:
            - echo hello
