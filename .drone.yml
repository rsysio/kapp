workspace:
    base: /go
    path: src/github.com/rsysio/kapp

pipeline:

    build:
        image: golang:latest
        commands:
            - curl https://glide.sh/get | sh
            - glide install
            - go build

    publish:
        image: ubuntu
        commands:
            - echo hello
